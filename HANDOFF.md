# 引き継ぎメモ

- **更新日**: 2026-09-08
- **対象**: X YouTube Card（`x-youtube-card-open-in-browser.user.js`）

このファイルは、新しいセッションが状況を把握するためのもの。
作業が一段落したら削除してよい。

---

## 1. 現在の状態

| 項目 | 状態 |
|---|---|
| リポジトリ | `hirodiver/tampermonkey-scripts`（public / デフォルト `main`） |
| スクリプト版数 | v3.8.3（`@name` を短縮） |
| 自動更新 | 有効（`@updateURL` は `main` の raw URL を指す） |
| 自動検証 | `test/x-youtube-card.test.js` 49項目 全通過 |
| **実機での動作確認** | 一部あり（下記2.5/2.6の実機報告を参照）。網羅的な確認はまだ |

配布URL:

```
https://raw.githubusercontent.com/hirodiver/tampermonkey-scripts/main/x-youtube-card-open-in-browser.user.js
```

## 2. 直近のセッションでやったこと（v3.4.0 〜 v3.8.0）

実機検証ができないため、代わりにヘッドレスChromium（Playwright）上へ
Xのカード構造を模したDOMを組み、スクリプトを実際に流し込んで検証した。
その結果、**3件の不具合が再現したので修正した**。

| 内容 | 症状 |
|---|---|
| React探索が境界要素自身を見ていた | 1 article に複数のYouTubeカードがあると、共有親のpropsを拾って**全カードが同じURLになる** |
| DOM再利用でボタンの `href` が残る | 仮想リストで別ツイートに化けたあと、**中クリック・リンクコピーで前の動画が開く** |
| DOM再利用後に先読みが発火しない | observe 済みの要素への再 observe は無視されるため、状態を作り直しても先読みが二度と走らない |

**2巡目（v3.5.0）でさらに2件:**

| 内容 | 症状 |
|---|---|
| `findStatus()` が article 全体の最初の status リンクを使っていた | **引用ツイート内のカードで外側のツイートIDを引き、別の動画が開く** |
| ドメイン表記の判定が完全一致だった | `From youtube.com` / `🔗youtube.com` 等の装飾付き表記を検出できずボタンが出ない |

あわせて改善した点:

- React探索にカード内子孫の走査を追加（最大40要素）
- カードが1枚だけの article では上位探索の境界を article の親まで広げた
- URLが未解決または weak のときは `href` を外すようにした
- `cardIndex()` と本文 t.co の探索範囲も scope（カードが属するツイート）基準に統一

## 2.5 実機報告からの修正（v3.6.0）

v3.5.0 配信後、実機（デスクトップChrome + iOS Safari 双方）から
**「カードをクリックして展開すると、ボタンがどこにも見えなくなる」**という
報告があった。これは模擬DOMでは再現していなかった、実機由来の不具合。

**原因**: `scan()` が毎回 `isYouTubeCard()` を再判定していた。カード展開直後の
一瞬、ドメイン表記が消えiframeもまだ無い過渡状態で判定がfalseへ反転し、
`scan()` がそのカードを丸ごと無視。React側は展開時に古いボタンを道連れに
消してしまうため、ボタンが跡形もなく消えていた。

**修正**: 一度trueと判定したカードは `state.isYtCard` に記憶し、同じツイート
である間は再判定しないようにした。あわせて、展開後の埋め込みiframeが
`youtube-nocookie.com` の場合にも対応（保険）。

修正前のコードで実際にこのシナリオが再現することを確認した上で修正した。

## 2.6 画像付き投稿への対応 → 実機で破壊、ロールバック（v3.7.0 → v3.7.1）

ユーザー要望: 画像とYouTube URLを含む投稿は、Xがカード化しない
（画像がサムネイル代わりになる）。しかしYouTubeを開く操作は常に
同じボタンで統一したい。

**方針はユーザーに確認して決定**（AskUserQuestionで2点確認済み）:

1. ボタンを出すタイミング → **確認後**ではなく、本文の表示テキストに
   YouTubeらしき文字列が見えている時点で即座に出す
2. 設置場所 → **画像の右上**（overlay）

**実装（v3.7.0）**: t.co自体は展開しないと遷移先が分からないが、Xは
本文リンクの表示に元URL（収まらなければ末尾省略）をテキストとして使う。
これを `findYouTubeTextLink()` で読み取り、動画IDが完全なら確定URL、
省略されていればweak扱い（クリック時にAPIで確定）とした。

画像コンテナ（`tweetPhoto`）自体を「card」として既存の状態管理・
先読み・クリック処理・開き方の仕組みにそのまま載せる設計。模擬DOMの
検証は41項目すべて通過し、`main` にマージして配信した。

**実機報告（v3.7.0配信後）**: 「画像が真っ白になり、ボタンも表示されない」。
模擬DOMでは再現しなかった、実機由来の破壊的な不具合。**ユーザーの指示で
即座に機能ごとロールバックした**（v3.7.1。中身はv3.6.0相当、`@version`
だけ前進——Tampermonkeyの仕様上バージョンを下げると自動更新が効かないため）。

原因は**未特定**。この開発環境からは実際のXのDOMを確認できず、以下は
疑いに留まる（`SPEC.md` 13章 v3.7.1に記載）:

- `tweetPhoto` 要素に `position: relative` を付与したことが、Xの画像
  レイアウトを壊した
- `tweetPhoto` セレクタが実際には `<img>` 要素自体を指しており、
  `appendChild`（子ノードを持てない）が例外を投げた

**教訓**: 実際のDOM構造を確認できない要素（今回のtweetPhoto）を新規に
操作対象にする変更は、模擬DOMのテストが通っても実機で全く違う結果に
なりうる。次に画像付き投稿対応を再挑戦するなら、まず実機のDevToolsで
`tweetPhoto` の実物（タグ名・親子関係・CSS）を確認してから設計すること。

## 2.7 画像付き投稿への再挑戦：画像に触れない設計に変更（v3.8.0）

ユーザー提案: 「画像に手を加えようとしたのがまずかったかもね。テキストの
最下部に追加だけならどうだろう」。実機のDevToolsで確認する手段が無い状況は
変わらないため、**確認より先に、そもそも壊しようがない設計に変える**方針。

**変更点**: ボタンの設置先を `tweetPhoto`（画像）から `tweetText`（本文）に
変更。`tweetPhoto` は「画像が付いているか」の判定にのみ使い、要素そのものは
一切操作しない（クラス付与もDOM挿入もしない）。ボタンは本文の直後に、
カード展開後のinline配置と同じ方式（独立ブロック要素の挿入）で追加する。
向きだけ逆（カードは要素の**前**、今回は要素の**後**）にするため、
`addButton()` の配置指定を真偽値から3値（`'overlay'` / `'inline-before'` /
`'inline-after'`）に拡張した。

検出・URL解決のロジック（`findYouTubeTextLink` / `resolveFromPostText`）は
v3.7.0から変更していない。壊れていたのは「画像への設置方法」であって、
「本文からの判定ロジック」ではないと判断したため。

模擬DOMで45項目通過（画像要素のHTMLが操作前後で完全に一致することを
確認するテストを追加）。ただし**これも実機未確認**である点はv3.7.0と同じ。
`tweetPhoto` を操作しなくなった分、v3.7.0のような破壊は起きにくいはずだが、
保証ではない。配信後は必ず画像付き投稿での見た目とクリック動作を確認する。

## 2.8 緊急停止フラグを追加（v3.8.1）

ユーザーから「配信して、うまくいかなかった場合に、ロールバック不要で
元に戻せるか」と問われた。正直に答えると、Tampermonkey自体には
バージョンを巻き戻す機能は無い。応急処置として「スクリプトを丸ごと
無効化」はいつでも可能だが、それだとカード機能まで止まってしまう。

そこで `ENABLE_IMAGE_POST_SUPPORT`（既定 `true`）を追加した。これを
Tampermonkeyのエディタで `false` に書き換えて保存するだけで、
画像付き投稿対応（v3.8.0で追加した機能）だけを即座に無効化できる。
git操作・push・CDNキャッシュ待ちが不要で、カード機能には影響しない。

**もし画像付き投稿で問題が起きたら**、まずこのフラグを `false` にして
応急処置し、その後で（急がず）このセッションかフォローアップに
状況を伝えてもらえば、コード側の恒久対応を検討する。

## 2.9 配信前カードのタップで復活しなくなる不具合を修正（v3.8.2）

ユーザーから実機のTampermonkey一覧のスクリーンショットとともに2点の
指摘があった。1点はスクリプト名が長くてバージョンが確認しづらいという
表示上の話（→ 2.10）、もう1点が本題：

> 配信前のカードをタップした際の直上にボタンが表示される機能が死んでる

確認したところ、**タップ前はボタンが出るが、タップ（展開）すると消える**
症状だった。「v3.6.0で直したはずの症状の再発」に見えたが、詳しく調べると
別の原因だった。

**原因**: v3.6.0の `isYtCard` 固定化は、「同じDOM要素の中身だけが変わる」
ケース（展開でドメイン表記が消える等）は救うが、**Xがカード要素自体を
丸ごと新しいノードに置き換える**ケースには無力だった。新しい要素には
`cardState` の記憶が無く、置き換え直後の一瞬（判定基準を満たさない過渡
状態）に `isYouTubeCard()` がfalseを返すと、その要素は二度とYouTube
カードとして扱われなかった。

再現用のPlaywrightシナリオ（カード要素を丸ごと別ノードに置き換える）を
組んで確認したところ、**現行コードでも v3.6.0（`ca31824`）のコードでも
同じ症状が再現した**。つまり今回の一連の変更（v3.7.0〜v3.8.1）による
退行ではなく、v3.6.0の修正が最初からカバーしていなかった別の穴だった。

**修正**: `articleYtState`（article単位で「YouTubeカードが確認された
実績がある」ことを記憶するWeakMap）を追加。同じarticle内の新しいカード
要素が判定基準を一時的に満たさなくても、そのarticleが既に確定済みなら
通すようにした。詳細は `SPEC.md` 8章。

トレードオフとして、1 article に複数カードがあり片方が無関係な別サービス
だった場合、誤ってYouTube扱いされる可能性がある（→ B-11）。article を
またいだ誤検出は起きないことをテストで確認済み。

検証を49項目に拡充。

## 2.10 スクリプト名を短縮（v3.8.3）

上記2.9のスクリーンショットで指摘された、もう1点。Tampermonkeyの一覧画面は
名前が長いと省略され、しかも一覧にはそもそもバージョンが表示されない
（個別のスクリプト詳細画面を開く必要がある）。

`@name` を変更した場合のTampermonkeyの実際の挙動（既存インストールの
名前が更新されるだけか、別スクリプトとして二重登録されうるか）は、
iOS版に絞ってWebSearchでも調べたが確証を得られなかった。一度は
リスクを理由に保留を提案したが、**ユーザーから「いいから修正して」と
明示的な指示があったため実行した**。

`@name` を `X YouTube Card - Open in Browser` → `X YouTube Card` に短縮
（`@namespace` は `local.hiro.tools` のまま変更していない）。ドキュメント
上の表記も合わせて統一した。

**もし実機で二重にインストールされたら**、Tampermonkeyのダッシュボードで
重複を確認し、古い方（またはどちらか一方）を削除する。実機でどちらの
挙動になったかは、まだ確認できていない。

対象は4スクリプトのうち `x-youtube-card-...` だけが自分の管轄。他3つ
（`x-status-page-auto-reload.user.js` / `youtube-full-dates-jst.user.js` /
`youtube-upcoming-stream-list.user.js`）も同じ指摘（名前が長い）に該当
しうるが、別セッションの管轄なのでそちらは今回対象外。ユーザーが望む
なら、該当セッションに依頼するか、今回の実機での結果を見てから判断する。

## 3. 検証の状況

**やったこと**

```
node --check x-youtube-card-open-in-browser.user.js
NODE_PATH=$(npm root -g) node test/x-youtube-card.test.js
```

`test/x-youtube-card.test.js` はヘッドレスChromium上の模擬DOMで49項目を確認する。
`GM_xmlhttpRequest` と `window.open` はモック。詳細は `SPEC.md` 第12章。

**このテストが保証しないこと**

Xの実DOM構造、Reactの実際の内部形状、iOSのUniversal Linkの挙動、
Tampermonkeyの `@connect` 判定、実APIのレスポンス形状。
**実機確認の代替にはならない。**

**やっていないこと（重要）**

v3.6.0時点で「展開後にボタンが消える」不具合、v3.7.0で「画像付き投稿対応が
画像とボタンを壊す」不具合、いずれも実機報告から見つかっている。ただし
網羅的な実機確認はまだ行っていない。以下を確認してほしい。

1. タイムラインのYouTubeカードにボタンが出るか
2. 押すとSafariでYouTubeが開くか（アプリに奪われないか）
3. 配信前のライブカードでURLが取れるか
4. 1つの投稿に複数のYouTubeカードがある場合、それぞれ正しいURLが開くか
5. タイトルに「YouTube」を含む他ドメインのカードにボタンが出ていないか
6. カードをクリックして展開したあとも、ボタンが残っているか（v3.6.0で修正済み）
7. **[v3.8.0・最優先]** 画像付きでカード化されない投稿（YouTube URL + 画像）で、
   画像が正常に表示されるか（v3.7.0はここが壊れていた）。本文の下にボタンが出るか、
   開いた動画が正しいか
8. **[v3.8.0]** 画像付きでもYouTubeと無関係な投稿にボタンが出ていないか

**ボタンが一切出ない場合**、疑う順序は
`[data-testid="card.wrapper"]` の変更 → `isYouTubeCard()` の判定が厳しすぎる、の順。
判定を緩めるなら `hasYouTubeDomainLabel()` を見ること。

## 4. 残っている制約

`SPEC.md` の第10章に記載。要点のみ:

| # | 内容 |
|---|---|
| B-5 | 複数カードへのURL割当が「article内の出現順」依存。API側の順序と食い違うと入れ替わる |
| B-6 | ドメイン表記の判定は表示仕様依存。Xが表記をやめると配信前カードを検出できない |
| B-7 | React探索の子孫走査は先頭40要素まで。巨大なカードでは取りこぼす |
| B-8 | 引用ブロックに status リンクが無い構成では、引用カードが外側のツイートIDで解決される |
| B-9 | 画像付き投稿の表示テキストからの動画ID復元は未検証のまま使う。稀に誤ったIDを開く可能性がある |
| B-10 | 画像付き投稿は `tweetPhoto`（判定用）と `tweetText`（設置先）の2セレクタに依存 |
| B-11 | `articleYtState` は article 単位で判定を緩める。複数カードのarticleで無関係な別サービスも誤検出しうる |
| C-3' | ダークテーマ追従は `prefers-color-scheme` ベース。X側だけの切替には追従しない |
| V-1 | 検証は模擬DOMのみ。実機確認は別途必要 |

## 5. 運用上の注意

**更新を配信するとき**

1. スクリプトを編集する
2. 自動検証を通す（上記2コマンド）
3. **`@version` を上げる**（上げないと誰にも届かない）
4. `SPEC.md` の変更履歴を更新する
5. `main` にプッシュする

**push した直後は実機に届かない。理由が2つある。**

1. Tampermonkey の定期チェックは既定でおおむね1日1回
2. `raw.githubusercontent.com` は `cache-control: max-age=300`、しかも
   **キャッシュは CDN の POP ごとに独立**。push 直後は端末の近くの POP が旧版を返しうる

2 のとき、Tampermonkey は取得した `@version` が同じなので
**「更新を確認」を手動実行してもエラーを出さず「更新なし」扱いになる**。
**push から数分おいてから実行すること。**

**待たずに済ませる確実な手順（実測で確認済み）**

配布URLを Safari で開き、インストール画面が出たら**インストールせずに閉じる**。
これで端末が新しいファイルを取り直すので、直後の「更新を確認」が通る。

実際に起きたこと（2026-09-07）:

| 順 | 操作 | 結果 |
|---|---|---|
| 1 | 編集画面 → 設定 → 更新 | 「更新はない」 |
| 2 | （数分経過）配布URLを Safari で開く | インストール画面が出た＝新版を取得できる状態 |
| 3 | インストールせず、1 とまったく同じ操作 | **更新成功** |

同一操作で結果が変わった以上、原因は取得内容（＝キャッシュ）である。
Tampermonkey は取得に成功した上で `@version` が同じと判断していただけで、
更新機能そのものは最初から正常だった。

開発環境から `curl` で配信URLを叩いて新バージョンが返っても、
**実機に届くことの証明にはならない**（別の POP を見ている）。
一度これで「配信は正常」と誤って断定した。

**同じリポジトリを複数セッションで触らない**

このリポジトリには別スクリプト（`youtube-full-dates-jst.user.js`）が
別セッションから追加されており、一度 README が衝突した。
作業前に必ず `git fetch origin main` で最新を取り込むこと。

**作業ブランチについて**

配布に使っているのは `main` だけ。作業ブランチにコミットしただけでは配信されない。
