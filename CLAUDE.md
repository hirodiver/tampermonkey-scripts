# CLAUDE.md

flowd のユーザースクリプト置き場（Tampermonkey、単一ファイル、`@grant none`）。

## 応答・記述の言語

- 会話、コード内コメント、UI 文言はすべて**日本語**。
- 変数名・関数名は英語（lowerCamelCase）。

## 作業の進め方（毎回適用）

### 提案で止めず、実行までやる

- 依頼されたことは、**最後までツールで実行して完了させる**。
  「これから調べます」「こう直します」で終わらせない。
- 細かい判断は自分でする。情報が足りなければ、まずファイル・履歴・
  ツールから読み取れないか調べる。どの選択肢でも意図を満たすなら、
  一番妥当なものを選んで進める。
- ユーザーに聞くのは次の場合だけ:
  - 解釈の違いで**結果が大きく変わる**とき
  - どうやっても情報が得られない・推測できないとき
  - 破壊的、外部に公開される、取り消しにくい操作のとき
- 確認を取るときは、選択肢ボタン UI ではなく**通常のテキストで質問**する。

### 完了報告は、実際に確認できたことだけ

- 「やるつもり」「試した」と「実際に実行できた」「結果を確認した」を
  厳密に区別する。前者を後者として報告しない。
- 失敗した、または結果を確認できなかったときは、そう書く。
- 渡す前に必ず構文チェックを**実行して**、その結果を報告する。

  ```
  node --check path/to/script.user.js
  ```

## 書き方

- 全体を `(function () { 'use strict'; ... })();` で囲む。インデント4スペース。
- `innerHTML` は使わない（Trusted Types）。`createElement` + `textContent`。
- セレクタは配列にして新旧を順に試す。取れなければ日本語のフォールバック文字列。
- SPA 対応3点セット: サイト固有の遷移イベント / MutationObserver / デバウンス。
- 日時表示は JST 固定（`Intl.DateTimeFormat('ja-JP', { timeZone: 'Asia/Tokyo' })`）。
